# Todo — Rendre `lms` (LM Studio CLI) découvrable depuis le plugin

## Objectif
Corriger `zsh:1: command not found: lms` (exit 127) au lancement de `lms` depuis Obsidian.

## Cause racine
Le PTY backend lance le shell en `-lc` (login non-interactif) → ne source pas `~/.zshrc`,
où l'installateur `lms` ajoute `~/.lmstudio/bin` au PATH. `getShellEnv()` (main.ts) ne
listait pas ce chemin.

## Étapes
- [x] Ajouter `~/.lmstudio/bin` à `extraPaths` dans `getShellEnv()` (main.ts).
- [x] Rebuild (`npm run build`) → chemin présent dans `main.js`.
- [x] Vérifier `~/.lmstudio/bin/lms` existe.
- [x] Bump version 0.2.15 → 0.2.16 (version.md, manifest.json, package.json, versions.json).
- [x] Entrée releases.md + memory.md.
- [x] Lint OK, 74 tests OK, build OK.
- [x] Commit + push.

## Review
Correctif ciblé, 1 ligne fonctionnelle. `mergePathEntries` déduplique déjà, aucun autre
changement nécessaire. Statut : completed.

## Vérification utilisateur
Recharger le plugin dans Obsidian puis relancer `lms chat` depuis une session : le message
"command not found" ne doit plus apparaître.
